# Host directory containing a Hugging Face cache with the pinned Faster-Whisper revision.
# Use forward slashes on Windows and never commit the real .env.docker file.
FACEFIT_STT_MODEL_CACHE_HOST=C:/replace/with/local/huggingface-cache
