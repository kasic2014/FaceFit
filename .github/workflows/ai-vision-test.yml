name: AI Vision Server Test

on:
  push:
    branches:
      - main
    paths:
      - 'ai-server/vision-server/**'
  pull_request:
    branches:
      - main
    paths:
      - 'ai-server/vision-server/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          cd ai-server/vision-server
          pip install -r requirements.txt
